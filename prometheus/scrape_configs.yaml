scrape_configs:

  - job_name: rippled
    scrape_interval: 3s
    scrape_timeout: 2s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - <host>:9998   # peer exporter
          - <host>:9999   # validator exporter
        labels:
          source: rippled
